# Implementation Plan: NN Training Signal Improvement

**Branch**: `014-nn-training-signal` | **Date**: 2026-03-14 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/014-nn-training-signal/spec.md`

## Summary

Refactor-first approach to improving NN training signal for aircraft path-following. Phase 1 strips GP dependencies and simplifies autoc into a standalone NN-only system. Phase 2 adds training improvements (sigma floor, curriculum, fitness decomposition, sep-CMA-ES). The refactoring enables fast iteration on training approaches by removing modal branching, GP infrastructure leakage, and unnecessary complexity.

## Technical Context

**Language/Version**: C++17 (g++, CMake 3.10+)
**Primary Dependencies**: Eigen3 (linear algebra), AWS SDK (S3), inih (config parser, vendored). Boost removed during refactoring.
**Storage**: S3 for evolution archives, local files for diagnostics (data.dat, data.stc)
**Testing**: GoogleTest 1.14.0, ctest
**Target Platform**: Linux (Ubuntu 22.04), Arduino/Xiao BLE Sense (xiao-gp export)
**Project Type**: CLI (evolution engine + simulation workers + tools)
**Performance Goals**: 50 population x 49 scenarios = 2,450 sims per generation; wall-clock target: < current 5000-pop GA
**Constraints**: 531-dimensional weight space, NN01 serialization format preserved, sensor-in/control-out contract preserved
**Scale/Scope**: 3 repos (GP/autoc, CRRCSim, xiao-gp), ~15k LOC in autoc

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### I. Testing-First
- **PASS**: Contract tests added before GP removal. All refactoring steps verified by existing + new tests.
- **Note**: Tests always build and run — no conditional compilation.

### II. Build Stability
- **PASS with modification**: Build verification changes to reflect NN-only fork:
  - GP: `cd ~/GP/autoc && bash rebuild.sh` (standalone, no parent `make`)
  - CRRCSim: `cd ~/crsim/crrcsim-0.9.13/build && make`
  - xiao-gp: `cd ~/xiao-gp && pio run`
- Constitution build commands will need updating after autoc becomes standalone.

### III. Dual-Mode Parity
- **EXEMPT (justified)**: GP tree evaluation and bytecode interpreter are being removed. This principle applies to GP-era code only. The NN fork has a single evaluation mode (nn_forward). The surviving parity requirement is: autoc training produces the same results as nnextractor → nn2cpp → xiao-gp deployment.

### Post-Design Re-check
- Testing-First: PASS — Contract tests defined for evaluator, config, evolution engine, RPC transport
- Build Stability: PASS — All 3 repos build at each phase boundary
- Dual-Mode Parity: EXEMPT — Single NN evaluation mode

## Project Structure

### Documentation (this feature)

```text
specs/014-nn-training-signal/
├── plan.md              # This file
├── research.md          # Phase 0: dependency analysis, config parser, CMA-ES, Boost
├── data-model.md        # Phase 1: entities and relationships
├── quickstart.md        # Phase 1: build and run guide
├── contracts/
│   ├── rpc-transport.md # Binary wire format (autoc <-> minisim)
│   ├── nn-serialization.md # NN01 format + S3 archive layout
│   └── config.md        # autoc.ini key reference
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (target layout after refactoring)

```text
autoc/
├── CMakeLists.txt
├── include/autoc/
│   ├── nn/
│   │   ├── nn_topology.h
│   │   ├── nn_genome.h
│   │   ├── nn_population.h
│   │   ├── nn_forward.h
│   │   └── nn_serialization.h
│   ├── eval/
│   │   ├── nn_evaluator_portable.h
│   │   ├── aircraft_state.h
│   │   └── simulation.h
│   └── util/
│       ├── config.h
│       ├── logger.h
│       ├── s3_archive.h
│       └── data_output.h
├── src/
│   ├── nn/
│   ├── eval/
│   ├── util/
│   ├── autoc.cc          # Main evolution executable
│   └── minisim.cc        # Simulation worker
├── tools/
│   ├── nnextractor.cc
│   ├── nn2cpp.cc
│   └── renderer.cc
├── tests/
│   ├── nn_evaluator_tests.cc
│   ├── nn_population_tests.cc
│   ├── nn_serialization_tests.cc
│   ├── config_tests.cc
│   ├── rpc_transport_tests.cc
│   └── cmaes_tests.cc
├── third_party/
│   └── inih/             # Vendored config parser (~400 LOC)
└── autoc.ini
```

**Structure Decision**: Standard C++ project layout with `include/autoc/` for public headers (namespaced subdirs: nn/, eval/, util/), `src/` for implementation, `tests/` for all tests, `tools/` for secondary executables. All file moves via `git mv` to preserve history.

## Implementation Phases

### Phase A: Dependency Analysis & Contract Tests
1. Full dependency analysis: `nm libgp.a`, transitive include tracing, symbol scanning
2. Contract tests for: evaluator (sensor-in/control-out), config (key parsing), evolution loop (gen-over-gen), RPC transport (send/receive)
3. These tests define surviving behavior — refactoring must not break them

### Phase B: Strip GP, Replace Config, Simplify Transport
1. Remove GP tree eval (autoc-eval.cc), bytecode interpreter (gp_bytecode.cc/h), gpextractor from autoc
2. Break inheritance: MyGP no longer inherits GP, MyPopulation no longer inherits GPPopulation, MyGene no longer inherits GPGene — replace with standalone classes or thin wrappers around NNPopulation
3. Replace GPConfiguration/GPVariables with inih (vendor into third_party/) — affects config_manager.h/cc, autoc.cc, nnextractor.cc
4. Replace GPrand()/GPsrand() with std::mt19937 — affects autoc.cc, pathgen.cc, gp_math_utils.h
5. Remove all `#include "gp.h"` and `#include "gpconfig.h"` from 11 files (see research.md table)
6. Remove libgp.a link, `include_directories(../include)`, `link_directories(../lib)` from CMakeLists.txt
7. Remove GP deserialization from minisim.cc, renderer.cc (these only need NN archive reading)
8. Strip modal if(GP)/if(NN) branches — happy path only
9. Verify: autoc, minisim, renderer all build and run with `ctest`

### Phase C: Replace Boost (trivial components)
1. boost::thread → std::thread
2. boost::mutex/cv → std::mutex/cv
3. boost::format → sprintf/std::format
4. boost::date_time → std::chrono
5. boost::process → fork()/execv()

### Phase D: Replace Boost (medium components)
1. boost::asio → POSIX sockets wrapper (~150 LOC)
2. boost::log → simple custom logger
3. boost::iostreams → std::stringstream

### Phase E: Replace Boost (serialization) + Simplify Transport
1. Custom binary RPC format (see contracts/rpc-transport.md)
2. Remove Boost serialization from EvalData, EvalResults, AircraftState
3. Single unified transport for train and eval
4. NN01 format preserved (already Boost-free)

### Phase F: Source Reorganization
1. Create include/autoc/{nn,eval,util}/ directory structure
2. `git mv` all headers and source files
3. Update CMakeLists.txt include paths
4. Update all #include directives
5. Move test binaries to build/tests/

### Phase G: Cross-Repo Updates
1. CRRCSim: update minisim RPC to match new transport format
2. xiao-gp: verify nnextractor → nn2cpp → build pipeline
3. Remove #ifdef GP_BUILD guards from shared evaluator code

### Phase H: Run Output Management
1. Configurable output directory (auto-created per run)
2. Remove hardcoded eval- prefix
3. All run artifacts (data.dat, data.stc, logs, weights) in run subdir

### Phase I: Repo Extraction
1. Extract autoc/ to standalone repository
2. Verify standalone build (no parent GP references)
3. Migrate relevant specs/docs

### Phase J: Training Signal Improvements
1. Sigma floor (FR-001) — clamp mutation sigma to configured minimum
2. Curriculum scenario ramp (FR-002) — wire VariationRampStep to NN evolution
3. Per-scenario fitness decomposition (FR-003) — minimax/percentile aggregation
4. sep-CMA-ES optimizer (FR-004) — ~200-300 LOC custom implementation
5. Per-timestep fitness streaming (FR-007) — extend RPC response
6. Per-segment credit assignment (FR-005) — segment scoring on timestep data
7. Checkpoint/resume (FR-008) — save full evolution state per generation
8. Behavioral cloning bootstrap (FR-006) — supervised MSE from GP flight data

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| Dual-Mode Parity exemption | GP tree eval and bytecode being removed | NN fork has single evaluation mode; parity is between training and deployment (autoc <-> xiao-gp) |
| Constitution build commands changing | autoc becomes standalone | Will update constitution after repo extraction |
